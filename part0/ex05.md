```mermaid
sequenceDiagram
    autonumber
    actor U as Usuario
    participant N as Navegador
    participant S as Servidor

    U->>N: Preenche o formulario e clica em "Salvar"
    N->>:S POST /newnote (payload JSON)
    S-->>N: HTTP 201 Created (sem redirecionamento)

    Note over N: O navegador nao recarrega a pagina<br/>e atualiza a visualizacao dinamicamente
    N->>U: Exibe a nova nota na interface
```